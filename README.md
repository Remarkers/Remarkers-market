# DOT-721 NFT marketplace

### Prerequisite
Node-js installed

VS code installed or github codespace

### Setting up
Clone this repo using `git clone` or Fork

Run `Npm install` or `npm i`

RUN `npm run dev`
Review in your local host
